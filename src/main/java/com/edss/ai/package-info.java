/**
 * AI domain: assistants, model orchestration, embedding stores. Skeleton —
 * module boundary reserved per {@code 03-domain-model.md}; no code yet. Owns
 * the {@code ai} Postgres schema when populated.
 */
@org.springframework.modulith.ApplicationModule(displayName = "ai")
package com.edss.ai;
