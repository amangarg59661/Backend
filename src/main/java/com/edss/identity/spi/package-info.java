/**
 * Public identity read + provisioning port. Other modules depend on this
 * package instead of {@code identity.domain} / {@code identity.application}.
 */
@org.springframework.modulith.NamedInterface("spi")
package com.edss.identity.spi;
