/**
 * Integrations domain: third-party connectors, webhooks, sync adapters.
 * Skeleton — module boundary reserved per {@code 03-domain-model.md}; no
 * code yet. Owns the {@code integrations} Postgres schema when populated.
 */
@org.springframework.modulith.ApplicationModule(displayName = "integrations")
package com.edss.integrations;
