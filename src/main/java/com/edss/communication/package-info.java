/**
 * Communication domain: inbound/outbound messaging, email threads, chat.
 * Skeleton — module boundary reserved per {@code 03-domain-model.md}; no
 * code yet. Owns the {@code communication} Postgres schema when populated.
 */
@org.springframework.modulith.ApplicationModule(displayName = "communication")
package com.edss.communication;
