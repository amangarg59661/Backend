/**
 * Organization domain: teams, departments, org-chart data. Skeleton — module
 * boundary reserved per {@code 03-domain-model.md}; no code yet. Owns the
 * {@code organization} Postgres schema when populated.
 */
@org.springframework.modulith.ApplicationModule(displayName = "organization")
package com.edss.organization;
