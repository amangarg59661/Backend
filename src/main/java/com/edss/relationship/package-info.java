/**
 * Relationship domain: contacts, accounts, relationship graph. Skeleton —
 * module boundary reserved per {@code 03-domain-model.md}; no code yet. Owns
 * the {@code relationship} Postgres schema when populated.
 */
@org.springframework.modulith.ApplicationModule(displayName = "relationship")
package com.edss.relationship;
