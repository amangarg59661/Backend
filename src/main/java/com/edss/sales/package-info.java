/**
 * Sales domain: pipeline, opportunities, quotes. Skeleton — module boundary
 * reserved per {@code 03-domain-model.md}; no code yet. Owns the {@code sales}
 * Postgres schema when populated.
 */
@org.springframework.modulith.ApplicationModule(displayName = "sales")
package com.edss.sales;
